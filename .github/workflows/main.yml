name: beopmusa deploy

on:
  push:
    branches: [ none ]

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: appleboy/ssh-action@v0.1.4
      with:
        host: beopmusa.cafe24.com
        username: beopmusa
        password: beopmusa1357!
        port: 22

